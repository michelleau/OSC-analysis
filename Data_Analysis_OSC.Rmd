---
title: "OSC_Data_Analysis"
author: "Aarthi Ravi"
date: "18/06/2021"
output: html_document
---
```{r setup}
require("knitr")
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_chunk$set(tidy.opts=list(width.cutoff=50),tidy=TRUE)

library(data.table)
library(tidyverse)
library(magrittr)
library(car)
library(gridExtra)
library(psych)
library(ggplot2)
library(multcomp)
library(plotly)
library(apaTables)
library(dplyr)
```

# Descriptives/ Preliminary Analyses

```{r data}
data <- read.csv ("OSC_cleaned.csv")
View (data)
data1 <- select (data, ID, AGE,BIRTH_EXP,DIS, REAC_TIME, ACCURACY)
View (data1)
summary (data1)
describe (data1)
```


```{r descriptives table}
d1 <- filter (data1, AGE < 9 )
d2 <- filter (data1, (AGE<12 & AGE>8))
d3 <- filter (data1, AGE>11)
d4 <- filter (data1, BIRTH_EXP == "V")
d5 <- filter (data1, BIRTH_EXP == "C")
d6 <- filter (data1, DIS == "Y")


number_group <- c(nrow(d1),nrow(d2),nrow(d3),nrow(d4),nrow(d5),nrow(d6))
part_demographics <- data.frame(cbind(nrow(d1),nrow(d2),nrow(d3),nrow(d4),nrow(d5),nrow(d6)))

names (part_demographics) <- c("Age_Group1 (6 to 8 yrs)", "Age_Group2 (9 to 11 yrs)", "Age_Group3 (12< yrs)", "Vaginally_born", "C-sec_born", "Gen_dis")


kable (part_demographics)
```


```{r descriptives table 2}
table (data1$BIRTH_EXP,data1$AGE_GROUPS)
table (data1$BIRTH_EXP,data1$DIS)
```

# Creating Factors
```{r Factors}
# Creating factors
data1$AGE_GROUPS <- factor(data1$AGE)
data1$AGE_GROUPS <- data1$AGE
data1$AGE_GROUPS[which(data1$AGE < 9)]<- "1"
data1$AGE_GROUPS[which(data1$AGE<12 & data1$AGE>8)]<- "2"
data1$AGE_GROUPS[which(data1$AGE > 11)]<- "3"

data1$AGE_GROUPS <- as.factor (data1$AGE_GROUPS)
data1$BIRTH_EXP <- as.factor (data1$BIRTH_EXP)
View (data1)
```

# Data Distribution Plots
```{r plots}
# Normal Distribution
qplot(AGE_GROUPS, REAC_TIME, data=data1, fill = BIRTH_EXP, geom = 'boxplot', main = "Mixed ANOVA")
qplot(AGE_GROUPS, ACCURACY, data=data1, fill = BIRTH_EXP, geom = 'boxplot', main = "Mixed ANOVA")
```

# Bar Graph Accuracy vs Age-Groups
```{r barplot}

ggplot(data1, aes(BIRTH_EXP, ACCURACY, fill = AGE_GROUPS)) + 
  geom_bar(stat="identity", position = "dodge") + 
  scale_fill_brewer(palette = "Set1")

ggplot(data1, aes(AGE_GROUPS, ACCURACY, fill = BIRTH_EXP)) + 
  geom_bar(stat="identity", position = "dodge") + 
  scale_fill_brewer(palette = "Set1")
```

There is a positive weak correlation , r = 0.4, between Reaction Time and Accuracy in trials. 


#TWO-WAY ANOVA
```{r ANOVA}
# IV- Birth Experience and Age Groups, DV - Reaction Time

model1 <- aov(REAC_TIME~BIRTH_EXP+AGE_GROUPS, data = data1)
summary (model1)

mod1 <- aov (REAC_TIME~BIRTH_EXP+AGE_GROUPS+BIRTH_EXP*AGE_GROUPS, data= data1)
summary (mod1)

# IV- Birth Experience and Age Groups, DV - Accuracy

model2 <- aov(ACCURACY~BIRTH_EXP+AGE_GROUPS, data = data1)
summary (model2)

mod2 <- aov (ACCURACY~BIRTH_EXP+AGE_GROUPS+BIRTH_EXP*AGE_GROUPS, data= data1)
summary (mod2)
TukeyHSD(mod2)

pairwise.t.test(data1$BIRTH_EXP,data1$ACCURACY,
                p.adjust.method = "BH")
```


```{r interaction}
plot(mod2, 1)
plot(mod2, 2)

interaction.plot(data1$AGE_GROUPS, ACCURACY, BIRTH_EXP, fixed = TRUE, type = "p")
```


